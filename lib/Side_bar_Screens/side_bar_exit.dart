import 'package:flutter/material.dart';

class side_bar_exit extends StatefulWidget {
  side_bar_exit({Key key}) : super(key: key);

  @override
  _side_bar_exitState createState() => _side_bar_exitState();
}

class _side_bar_exitState extends State<side_bar_exit> {
  @override
  Widget build(BuildContext context) {
    return Container(
       
    );
  }
}