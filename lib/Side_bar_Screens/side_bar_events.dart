

import 'package:flutter/material.dart';

class side_bar_events extends StatefulWidget {
  side_bar_events({Key key}) : super(key: key);

  @override
  _side_bar_eventsState createState() => _side_bar_eventsState();
}

class _side_bar_eventsState extends State<side_bar_events> {
  @override
  Widget build(BuildContext context) {
    return Container(
     
    );
  }
}