import 'package:flutter/material.dart';

class side_bar_faculty extends StatefulWidget {
  side_bar_faculty({Key key}) : super(key: key);

  @override
  _side_bar_facultyState createState() => _side_bar_facultyState();
}

class _side_bar_facultyState extends State<side_bar_faculty> {
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}