import 'package:flutter/material.dart';

class side_bar_admission extends StatefulWidget {
  side_bar_admission({Key key}) : super(key: key);

  @override
  _side_bar_admissionState createState() => _side_bar_admissionState();
}

class _side_bar_admissionState extends State<side_bar_admission> {
  @override
  Widget build(BuildContext context) {
    return Container(
       
    );
  }
}